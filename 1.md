你是 Codex。请在现有仓库 rule3d-main（SPIRAL3D / raven3d 生成器）基础上进行一次“定向重构”，把原来的多物体点云规则生成，改造成 PCRAR：基于 CSG 布尔树（布尔几何体）的关系/类比推理数据生成器。要求首版即终版：代码清晰、最小依赖（尽量仅 numpy）、输入输出格式统一、规则库精简为 7 条，并且与原项目 R1–R4 的含义保持“来源对齐”。

========================
0) 你必须先理解的“新旧差异”
========================
【原项目（rule3d / SPIRAL3D）】
- Scene = 多个独立 primitive（ObjectState 列表），点云 = 各 primitive 的 surface 采样后拼接。
- 题型主要是 2→1：给 A/B（1.ply/2.ply），从 4 个候选里选正确延续（3-6.ply）。
- 规则库庞大（R1-R4 共 50+），涉及复杂空间/多对象/动态过程。

【新项目（PCRAR）】
- 每个点云只包含 1 个复合实体 E：E = 布尔组合后的几何体点云（不是多个 primitive 的简单拼接）。
- E 的定义：Attr(E) = (CSG, O)
  - CSG：二叉布尔树；叶节点是 primitive（Sphere/Box/Cylinder/Cone）+ leaf 的局部属性
  - O：整体摆放与观测（全局 pose + 观测采样/密度配置等）
- 题型有两种：
  1) Relational（2→1）：输入 A,B，推断变换 T，使得 B=T(A)，然后从候选中选 D* = T(B)
  2) Analogical（3→1）：输入 A,B,C（B=T(A)），从候选中选 D* = T(C)
- 每题只允许 1 个主规则 T；绝大多数属性保持不变，只改 1–2 个属性轴。
- 规则精简为 7 条（Progression/Cycle/Copy/Count/Conservation/Permutation/Symmetry），并标注其对应原项目 R1-R4 的来源。

========================
1) 最终你要交付的东西（必须都做）
========================
A) 新的数据结构：CSG Tree + Attr(E) JSON schema（统一格式，可 round-trip）
B) CSG 布尔几何体点云采样：surface-only，n_points 固定为 8192，支持 Union/Intersect/Diff
C) 7 条规则的实现（只在离散属性轴上做变化），并能用于：
   - 生成 A,B,(C),D*
   - 生成 3 个干扰项（distractors）
   - 唯一性校验（候选中只有一个满足同一条规则模板+同一组参数）
D) 统一的输出目录结构 + meta.json schema（对 Relational 与 Analogical 都一致）
E) CLI：python main.py 能生成 PCRAR 数据（保留旧模式可选但默认切到 pcrar）
F) 文档：README 更新，说明新旧差异、属性轴、规则库、数据格式

========================
2) CSG Tree：统一数据结构（重点：布尔几何体）
========================
2.1 Primitive Types（只保留 4 种）
- Sphere, Box, Cylinder, Cone
说明：原项目 Cube 可以改名为 Box；其余 TriangularPrism/Capsule/Torus 等全部移除或不在 PCRAR 模式使用。

2.2 CSG Node（二叉树）
- Node = Leaf | Op(Node, Node)
- Op ∈ {Union, Intersect, Diff}
- leaf_count（PartCount）最多 3（你的实现必须默认最多 3，可配置但默认=3）

2.3 Leaf 的属性（局部属性必须写进 leaf）
每个 Leaf 必须带：
- prim_type: one of {sphere, box, cylinder, cone}
- size_level: one of {S, M, L} （离散三档）
- local_pose: 欧拉角离散（推荐：绕单轴 0/90/180/270；也可只用 90/180/270 做 step）
- local_position: slot-based 离散位置（推荐 x 轴 slot ∈ {-1,0,+1}）
- position_delta_level: one of {Near, Mid, Far} 映射到 δ ∈ {0.35, 0.50, 0.65}
  实际平移：t = (slot * δ, 0, 0)
（注意：Position 不是连续坐标；必须离散化并在 meta 里存 slot 与 δ 档位，避免浮点抖动）

2.4 O：全局摆放与观测（统一放在 Attr(E).O）
- global_pose: 离散旋转（同样用 0/90/180/270 的离散欧拉）
- global_translation: 默认 0（可以保留字段但 PCRAR 规则里不使用）
- sampling_mode: 固定 "surface"
- n_points: 固定 8192
- part_sampling_weights: 密度离散档（见下文 Density 属性）

2.5 JSON Schema（你要实现 to_dict/from_dict）
建议 schema（可微调，但必须稳定一致）：
Attr(E) = {
  "csg": Node,
  "obs": {
    "global_pose_deg": [rx, ry, rz],
    "global_translation": [tx, ty, tz],
    "sampling_mode": "surface",
    "n_points": 8192,
    "part_sampling_weights": [w1, w2, (w3)]
  }
}
Node:
- Leaf:
  {"type":"leaf","id":0,"prim":"sphere","size":"M",
   "local_pose_deg":[0,0,0],
   "slot":-1,
   "delta_level":"Mid"}
- Op:
  {"type":"op","op":"diff","left":Node,"right":Node}

同时额外提供：
- "expr": 方便人读的表达式字符串（例如 Diff(Union(Sphere,Cylinder),Box)），用于 debug 和 meta 展示。

========================
3) inside(x) 与布尔几何体 surface 采样（必须落地）
========================
3.1 必须为每个 Primitive 实现 inside(x)->bool（向量化更好）
- 先把世界坐标点 x 逆变换到 primitive 局部坐标：
  x_local = R^T * ((x - center) / scale)
- Sphere: ||x_local|| <= r
- Box: max(|x|,|y|,|z|) <= 0.5（或按边长定义）
- Cylinder: x^2+y^2 <= r^2 且 |z| <= h/2
- Cone: 线性半径随 z 变化（实现一个稳定的 inside 判定即可）

3.2 CSG inside(x) 递归
- Union: inside = inside(left) OR inside(right)
- Intersect: inside = AND
- Diff: inside = inside(left) AND NOT inside(right)

3.3 采样：仅 surface（而不是体素/体内采样）
目标：给定 Attr(E)，输出 N=8192 个点，近似落在布尔结果的边界表面。
推荐实现（轻依赖、可控）：
- step1: 按 part_sampling_weights 对每个 leaf 分配 proposal 数（比如先 proposal= N * oversample_factor）
- step2: 从每个 leaf primitive 的 surface 采样 proposal 点（你已有 sample_surface）
- step3: 对每个 proposal 点做“边界判定”：
  - inside_result(p) = CSG.inside(p)
  - 随机取 m 个小扰动方向 u，检查 inside_result(p) 与 inside_result(p + eps*u) 是否存在差异
  - 若存在差异 => p 在 boundary 附近，保留
- step4: 若点数不足 N，继续补采样（最多迭代 K 次，避免死循环）
- step5: shuffle 并输出 N 点

说明：
- Diff / Intersect 会产生“新边界”（不是简单拼接 leaf 表面），上述扰动法能近似捕获。
- eps 建议 = 1e-3 ~ 5e-3（和你的几何尺度一致即可）
- oversample_factor 建议 3~8

========================
4) 属性轴（离散）必须统一并写入 meta
========================
属性轴（7 个）：
1) Shape：由 leaf prim_type 集合 + CSG tree 共同定义（无需额外字段）
2) Boolean Ops：由 CSG tree 内部 op 节点定义
3) PartCount：leaf_count ∈ {2,3}（默认最多 3）
4) Size：每个 leaf 的 size_level ∈ {S,M,L}（映射到 scale 值，比如 {0.8,1.0,1.2}）
5) Pose：global_pose + 可选 leaf local_pose（都离散角度）
6) Position：slot ∈ {-1,0,+1} + delta_level ∈ {Near,Mid,Far}
7) Density：part_sampling_weights 的离散档位（总点数固定 8192）

Density 离散档建议：
- leaf_count=2: [[1/2,1/2],[2/3,1/3],[1/3,2/3]]
- leaf_count=3: [[1/3,1/3,1/3],[1/2,1/4,1/4],[1/4,1/2,1/4],[1/4,1/4,1/2]]
（你可以精简到 3 档即可，但必须稳定、可复现、写进 meta）

========================
5) 规则库：只保留 7 条（并标注来源对齐原 R1-R4）
========================
每题只用 1 条规则 T；且只改动 1–2 个属性轴。规则必须可用于 “apply(attr)->attr”。
实现以下 7 条规则（命名用一个词）：

Rule1 Progression（递进）
- 来源对齐：R1-1(size 等差)、R1-2/3(旋转等步/离散)、R1-4(位置等步)、R1-5(密度等差)
- 定义：同一属性沿固定步长变化：A→B 走一步，C→D 也走同一步
- 轴（四选一）：r(size_level +/-1)、R(pose +90deg 等)、p(slot shift +/-1 或 delta_level +/-1)、d(weights 档位 +/-1)

Rule2 Cycle（循环）
- 来源对齐：R1-6、R3-10（形状变化延续/多对象形状变化）
- 定义：某个 leaf 的 prim_type 做离散循环 Sphere→Box→Cylinder→Cone→Sphere

Rule3 Copy（拷贝）
- 来源对齐：替代原整个 R3 的“结构变换”复杂度
- 定义：随机选 1-2 个 leaf，将其尺寸/密度拷贝为另一 leaf（仅尺寸/密度变化）

Rule4 Count（增减）
- 来源对齐：R1-11、R3-4/5、R4-3（个数变化/吞噬/融合的简化统一）
- 定义：leaf_count: 2→3 或 3→2
- 注意：树形必须保持二叉结构；新增 leaf 时要生成新的 leaf 节点并插入树

Rule5 Conservation（守恒）
- 来源对齐：R2-2
- 定义：任选两 leaf：size(i)+size(j)=C（离散守恒，一增一减）

Rule6 Permutation（置换）
- 来源对齐：R3-2（ord_x 置换循环）、R3-7（位置轮换）
- 定义：把 leaf 当槽位对象做循环置换：
  - ShiftSlots：leaf 的 slot assignment 循环左移/右移
  - 或 PermuteLabels：按 ord_x（由 slot 决定）得到的排序标签置换（本质仍是 permutation）

Rule7 Symmetry（对称）
- 来源对齐：R4-7
- 定义：选一对 leaf（左/右）作为对称对：
  - A→B 左做 +Δ，则右做 -Δ（作用于 p / R / r / d 之一）
  - 第三个 leaf（若存在）可当锚点不动

========================
6) 题目生成：Relational 与 Analogical 都要支持
========================
6.1 Relational（2→1）
目标：让模型“认知属性/关系规律”
- 采样 Attr(A)
- 采样规则 T（含参数）
- 得到 B = T(A)
- 正确答案 D* = T(B)
- 候选 {D1..Dk}（默认 k=4），仅 D* 满足同一个 T

6.2 Analogical（3→1）
目标：把在 A→B 学到的关系规律迁移到新的几何体 C 上
- 采样 Attr(A)
- 采样规则 T
- B = T(A)
- 采样 Attr(C)（要求与 A 同分布、但不是 A 的 copy；且与规则前置条件兼容）
- D* = T(C)
- 生成候选，仅 D* 满足 T

6.3 干扰项（distractors）
- 生成 3 个干扰项：必须“看起来接近”，但不满足同一条规则模板+同一组参数
- 做唯一性校验：候选里只能有一个满足 T（用 rule.check 或直接比对 apply 的结果）

========================
7) 输出文件格式：统一且稳定（重点）
========================
必须统一：
- 点云统一使用 ASCII .ply（沿用原 write_ply）
- meta 统一 JSON（UTF-8）

建议新的样本目录（统一命名，易读）：
output/sample_000000/
  in_0.ply   # A
  in_1.ply   # B
  in_2.ply   # C（Relational 任务可省略；但 meta 里用 input_paths 列表统一表达）
  cand_0.ply
  cand_1.ply
  cand_2.ply
  cand_3.ply
  meta.json
output/meta.json  # list[meta.json] 汇总

meta.json schema（必须实现一致，不要再用 ref1_path/cand1_path 这种固定字段）：
{
  "id": "sample_000000",
  "task_type": "relational" | "analogical",
  "input_paths": ["sample_000000/in_0.ply", "sample_000000/in_1.ply", ...],
  "candidate_paths": ["sample_000000/cand_0.ply", ...],
  "gt_index": 0,
  "gt_label": "A"|"B"|"C"|"D",
  "n_points": 8192,
  "rule": {
    "template": "Progression"|"Cycle"|...,
    "source_align": ["R1-4", ...],   # 对齐原规则编号
    "params": {...}                  # 规则参数（轴、对象索引、步长、置换方向等）
  },
  "entities": {
    "inputs": [Attr(E)_json_for_A, Attr(E)_json_for_B, (Attr(E)_json_for_C)],
    "candidates": [Attr(E)_json_for_cand0, ...]
  },
  "notes": {
    "distractors": ["why wrong", ...]
  }
}

注意：
- entities 里必须存完整的 Attr(E)（包含 CSG tree + obs），这样点云可完全复现
- 点云颜色：可以沿用旧的 color_map，但必须保证同一文件内所有点同色（可选）

========================
8) 代码组织建议（要求你按这个做，避免混乱）
========================
- 保留原 raven3d/ 作为基础库，但为 PCRAR 新增：
  raven3d/csg.py            # CSGNode/Leaf/Op + inside() + to_dict/from_dict
  raven3d/pcrar_entity.py   # Attr(E) + sample_point_cloud(8192)
  raven3d/pcrar_rules.py    # 7 条规则：sample_params/apply/check
  raven3d/pcrar_dataset.py  # 生成 relational/analogical 样本与 meta
- main.py 增加 --mode pcrar（默认 pcrar），旧模式 main 保留但不再默认
- requirements.txt 不新增重依赖（不要引入 trimesh/pycsg 等重库）

========================
9) 自测（你必须写在 README 并保证可跑）
========================
- 生成 5 条 relational + 5 条 analogical：
  python main.py --mode pcrar --num-samples 10 --task-mix 0.5 --output output_pcrar --seed 0
- 检查：
  - 每个样本 meta.json 字段齐全
  - input_paths 与 candidate_paths 指向的 ply 文件存在
  - gt_index 合法
  - entities 中的 Attr(E) 可反序列化并复现点云（至少结构一致）
  - 唯一性校验通过（候选仅 1 个满足 T）

========================
10) 你输出给我的内容
========================
- 直接修改仓库代码（给出完整 diff/patch）
- 更新 README（说明新旧差异、属性轴、规则库、输出格式）
- 给一个最小可运行示例（命令 + 生成目录截图说明即可）

请开始实现。优先保证：CSG 布尔几何体的数据结构与点云采样正确且可复现；其次是 7 条规则与数据集格式的稳定性。
