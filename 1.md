你是一个工程型实现助手（Codex）。请在仓库 PCRAR-main/ 中对“出题/数据生成”做一次结构性改造：把原先的出题范式（relational / analogical 等）整体推翻并替换为“3×3 九宫格矩阵题（RAVEN-like）”作为唯一默认题型。旧模式可以保留为 legacy（可选开关/兼容读取），但训练/生成主路径、默认配置、默认脚本与输出格式都应以九宫格矩阵题为中心。

========================
1) 新的唯一题型：3×3 矩阵题（必须满足的逻辑）
========================
1.1 单一规则实例（避免互搏）
- 每一道题只采样并固定一个规则实例 T：同一个 RuleTemplate + 同一组 RuleParams。
- 规则参数在整张 3×3 内绝不能漂移：不能出现不同行/列 axis/direction/leaf/step 改变的情况。

1.2 行列“粒度不同”但同一规则
- 行与列的区别只能来自“同一 T 的不同幂次步长”：
  - 横向每走一步等价于应用 T 的 k_h 次
  - 纵向每走一步等价于应用 T 的 k_v 次
  - k_h、k_v 为本题固定整数（默认从 {1,2} 采样，可配置）
- 3×3 网格必须由指数公式生成：
  E[r,c] = T^(r*k_v + c*k_h)(E[0,0])
- 必须实现并通过路径一致性检查（防实现 bug）：
  从左上走“先右后下”和“先下后右”得到的中间格必须完全一致（例如 E[1,1] 同时等于 apply_k(E[1,0],k_h) 与 apply_k(E[0,1],k_v)）。

1.3 挖空与答案
- 目标格固定为右下角 (2,2) 并挖空，作为需要预测的目标。
- 生成器内部必须保留 GT 实体 E[2,2] 用于答案与候选构造；对外输出时该格应为空（或仅由 meta 标注为 missing）。

========================
2) Matrix 任务的离散档位扩展（必须做，避免 3 档限制卡死）
========================
背景：当前项目多个属性是“3 档固定”（SizeLevel S/M/L、Slots -1/0/1、DENSITY_POINT_PRESETS 三档、DeltaLevel 三档）。
九宫格最大幂次 K_max = 2*k_h + 2*k_v。若常用 k_h/k_v∈{1,2}，则 K_max=6；3 档 progression 极易越界/退化或迫使 k_h=k_v=1。

要求：
- 保持“离散档位”而非连续值（便于 entities_equal、候选构造与质量筛选；避免任务退化成回归估计）。
- 将档位扩展设计成“新默认矩阵题使用”，而不是旧模式专用：
  - 默认：matrix_size_levels=7
  - 默认：matrix_density_levels=5
  - 默认：matrix_delta_levels=5
  - matrix_slot_levels 初版可不扩（仍 -1/0/1），避免引入 slot 碰撞与复杂约束；如要扩必须可配置且默认不扩
- 允许用新的离散取值表/枚举实现（不要求改旧枚举，但主生成链路必须使用扩展档位）。

========================
3) 候选选项（多选一）生成逻辑（必须实现语义分层）
========================
输出为多选一（默认 4 选 1，可配置 num_options）。候选需要满足以下语义层级与可验证性：

3.1 正确答案（GT）
- 只且必须是 E[2,2]：唯一使用与源领域（同一题内部已见 3×3 其他格）共享的真实关系/规则实例 T 对目标格补全得到的实体。

3.2 “语义上合理但关系错误”的候选（Analogical-but-wrong-relation，至少 1 个）
- 这些候选在目标领域属性/风格上合理：与目标矩阵中已见格子的 primitive set、尺度/密度范围、slot 布局等一致，给人“补在这里也说得通”的感觉。
- 但它们不满足真实规则 T 在目标位置的推导约束（candidate != GT 且 check_true_relation(candidate)=false）。
- 关键：它们必须能被另一个一致但不同的关系 T'（或 T 的变体参数）对“目标领域上下文”进行一致补全解释：
  - 例如同模板但改 axis / direction / leaf_idx / step / cycle offset 等得到 T'
  - 并实现 check_alt_relation(candidate, T')=true（至少在目标最后一行或最后一列上形成明确一致的递推/循环/置换约束）
- 直观目标：让“只看目标域属性/外观”时多个候选都很像对的；只有做类比与关系推理才能挑出真正使用 T 的那个。

3.3 “感知合理但关系不一致”的候选（Perceptual-plausible，至少 1 个）
- 与目标域属性非常接近（例如从 GT 做轻微扰动或从目标域统计分布采样），人眼看很像。
- 但它不满足真实关系 T，也不满足你定义的任何“自洽替代关系”检查（check_alt_relation 全部失败或代价显著更高）。

3.4 其余不合理候选（Irrelevant）
- 明显不符合目标域属性与关系（primitive 集不一致、尺度/密度超范围、slot 越界、明显违背矩阵规律等）。

必须实现可程序化验证的接口（用来保证上面三类候选确实分层）：
- check_consistent_with_true_relation(candidate, context, T) -> bool（只有 GT 应为 true）
- check_consistent_with_alt_relation(candidate, context, T') -> bool（analogical_wrong_relation 至少 1 个为 true）
并在生成时用这些检查进行 rejection/重采样，保证候选语义真的成立。

========================
4) 工程改造方向（高层，不要逐步讲解）
========================
4.1 生成器主入口重构
- 把默认生成入口改为 matrix-only：例如 PCRARDatasetGenerator.generate_sample() 默认走矩阵逻辑；
- 原 relational/analogical 若保留，只能作为 legacy 代码路径，默认不启用、不参与主配置与主脚本。

4.2 配置与脚本迁移
- PCRARConfig 改为以 matrix 为中心的默认参数（k_h/k_v 候选、档位扩展、候选数量、distractor 配比、规则白名单等）。
- dataset 生成脚本/README/示例配置更新：默认跑 matrix 题并输出 matrix 格式目录结构。

4.3 核心实现模块（建议拆出清晰边界）
- matrix_grid.py 或同等模块：
  - apply_k(entity, T, k)
  - can_apply_k(entity, T, K_max)
  - generate_grid(E00, T, k_h, k_v) 按指数公式产出 3×3
  - grid_quality_checks(...)
  - relation_checks_true/alt(...)
- candidate_generation.py：
  - generate_candidates(grid_context, T, k_h, k_v, num_options, mix_cfg)
  - 标注每个候选类型（gt/analogical_wrong_relation/perceptual_plausible/irrelevant）

4.4 输出格式成为新标准
- 每个 sample 输出目录 sample_xxxxxx/：
  - grid_r_c.ply（对 (2,2) 可不输出或输出空占位）
  - cand_i.ply
  - meta.json（必须包含）：
    - grid_paths 3×3 表、target_position=(2,2)
    - rule_template、rule_params、k_h、k_v、K_max
    - matrix_level_config（记录 size/density/delta/slot 档位数或取值表）
    - candidate_paths、gt_index、distractor_types

4.5 质量与回归
- 新增 smoke test：批量生成 N 个样本，逐个验证：
  - 指数公式成立
  - 路径一致性成立
  - 行列相邻差异 + 全局多样性阈值成立
  - 候选集中恰好一个满足 true_relation（对应 gt_index）
  - 至少一个候选满足 alt_relation（被标记为 analogical_wrong_relation）
- 确保旧 legacy 路径（若保留）不影响新默认输出与训练流程。

========================
5) 复用与风格要求
========================
优先复用现有：
- PCRAREntity、entities_equal、write_ply、write_meta、ensure_dir
- pcrar_rules 的 RuleTemplate / RuleParams / get_rule / sample_applicable_rule
在必要处新增小型工具函数
