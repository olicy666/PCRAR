# PCRAR 点云矩阵推理项目文档（显式 `[RULE]` token + 预测误差 PE）

> 目标：在你的 **PCRAR 点云版 3×3 Raven 九宫格补全（4 选 1）**任务上，做一个**准确率尽量高**的模型。
> 核心思路：用**预训练点云 Encoder**提特征，用显式的 **`[RULE]` token** 做“规则潜变量瓶颈”，再用 **Prediction Error (PE)** 做候选验证与打分（谁让误差最小，谁更像正确规则补全）。

---

## 1. 项目要做什么（Problem / Deliverables）

### 1.1 任务定义

每个样本是一道 3×3 矩阵补全题：

* 9 个格子里有若干缺失（你的数据中常见：**每行缺一个**，且右下角为目标缺失）。
* 给出 4 个候选点云，选择唯一正确答案。

### 1.2 输入与输出

**输入**（来自 `output_matrix/meta.json` 与样本文件夹）：

* `grid_paths`: 3×3 列表，元素是 `.ply` 路径或 `None`
* `grid_observation_mask`: 3×3 观测掩码
* `missing_positions`: 缺失位置列表（包含 target）
* `target_position`: 目标位置（常见 `[2,2]`）
* `candidate_paths`: 4 个候选 `.ply`
* `gt_index`: 正确候选索引（0~3）
* 额外 meta：`rule_template / matrix_relation / rule_params / point_cloud_row_perturbation / distractor_types ...`（用于训练辅助或分析）

**输出**：

* `scores ∈ R^4`：每个候选得分（越大越好）
* 预测答案：`argmax(scores)`

### 1.3 交付物

1. 可训练的 PyTorch 训练/验证脚本（含 dataloader、模型、loss、指标）
2. 一个可复现实验配置（yaml/json）：点数、encoder、层数、学习率等
3. 评估报告：总准确率 + 分规则/分扰动类型准确率 + 关键消融
4. 最佳 checkpoint（用于推理）

---

## 2. 数据与输入预处理（从 PLY 到网络张量）

> 你这题的规则可能涉及 **位置、尺寸、密度、姿态、颜色/特征**。所以预处理的原则是：
> **既要让几何编码稳定（方便预训练模型吃），又不能把规则信息洗掉。**

### 2.1 PLY 读取与字段管理

每个点云读取为：

* `xyz_raw: (N,3)` float32
* `feat_raw: (N,F)`（可选：rgb/intensity/normal/…）
* `N_raw = N`（极重要：密度/点数常是规则的一部分）

建议：把 ASCII `.ply` **离线转为二进制**（`.npz` 或 `.pt`）以避免训练 I/O 成瓶颈。二进制内容建议包含：

* `xyz_raw, feat_raw, N_raw`
* 可选缓存：`bbox, centroid, color_stats` 等

### 2.2 坐标归一化：同时保留“全局关系”和“局部形状”

模型会同时使用两套坐标：

#### A) 题目级（全局）归一化：保留格子之间相对关系

对一整道题，把所有**已观测格子**点拼起来估计：

* 全局中心 `C_global`（如拼接点云均值）
* 全局尺度 `S_global`（如全局 bbox 对角线）

对题中所有格子/候选统一变换：

* `xyz_global = (xyz_raw - C_global) / (S_global + eps)`

这样不会破坏“位置/尺度的相对模式”。

#### B) 格子级（局部）坐标：让预训练 Encoder 更容易提“形状”

对每个格子点云（基于 `xyz_global`）计算：

* `C_cell = mean(xyz_global)`
* `S_cell = bbox_diag(xyz_global)` 或 `std`（二选一，保持一致即可）

构造：

* `xyz_local = (xyz_global - C_cell) / (S_cell + eps)`

> 注意：我们不会丢掉 `C_cell / S_cell / N_raw`，它们会作为**显式全局特征**输入推理网络。

### 2.3 点数统一（N0）：训练与预训练模型对齐

预训练点云模型通常要求固定点数 `N0`：

* 推荐：`N0=1024` 起步（资源够再试 2048）
* 若 `N_raw > N0`：随机采样（先用随机，FPS 后面再上）
* 若 `N_raw < N0`：重复采样 + `point_mask`（或 padding）

同时保留一个全局标量：

* `n_scalar = log(N_raw)`（密度/稀疏规则不会被下采样抹掉）

### 2.4 特性（多通道）处理策略

你的点除了 xyz 还有“很多不同特性”，建议分成两类：

1. **逐点特性**（RGB/intensity/normal/…）：放到点维度一起编码
2. **全局统计特性**（建议显式算出来）：

   * `log(N_raw)`
   * `C_cell(3), S_cell(1)`
   * `bbox_sizes(3)`
   * 若有颜色：`rgb_mean(3), rgb_std(3)`
   * 其他你认为可能参与规则的统计量（越像“规则变量”，越值得做统计）


## 3. 模型总体架构（High-level）

每道题包含：

* 6 个观测格子点云（9 格里缺 3 格的常见设定）
* 4 个候选点云

模型分三段：

1. **点云编码器（共享）**：每个点云 → 一个 embedding `z ∈ R^D`
2. **RuleFormer（规则推断 + 目标预测）**：显式 `[RULE]` token 输出规则向量 `r`，预测目标格 embedding `z_hat_tgt`
3. **VerifyFormer（候选验证，可选但强烈建议）**：把候选塞回矩阵，计算全局自洽 PE，输出候选得分

最终：选择 **让 PE 最小 / 得分最大** 的候选。

---

## 4. 点云 Encoder（先进预训练特征提取器 + 多特性融合），尽量不要去重新训练encoder，只把它作为点云特征的提取器

> 关键工程目标：**尽量不破坏预训练主干**，同时把你那些额外特性喂进去。

### 4.1 预训练几何主干

优先级（从“上手稳、迁移强”到“更重更复杂”）：

* **Point-MAE**（Masked AutoEncoder for point clouds）：稳定、迁移强、适合几何规则任务
* **Point-BERT**（Masked Point Modeling）：同级强者
* **Point-M2AE / 多尺度 MAE 系列**：更强但更复杂
* **PointDiff / Diffusion 预训练点云表征**：对噪声更鲁棒，但工程成本更高
* （可选）**ULIP-2**：语义更强，但你这题偏“规则/几何”，不一定最划算

> 实操建议：先用 **Point-MAE base** 跑通并取得强基线，再看是否需要更重的 encoder。

### 4.2 多特性融合：双分支（推荐）

#### 分支 A：预训练几何分支（尽量保持原接口）

输入：`xyz_local (N0,3)`
输出：`z_xyz ∈ R^D`（一般取 CLS token 或对 patch token pooling）

#### 分支 B：特性分支（从零训练，轻量）

输入：逐点 `[xyz_global, feat_raw]`（或仅 `feat_raw`）
结构：PointNet 风格 `MLP(point) + maxpool`
输出：`z_feat ∈ R^D`

#### 全局统计分支（强烈建议）

输入：`g = [log(N_raw), C_cell, S_cell, bbox_sizes, rgb_stats, ...]`
结构：小 MLP
输出：`z_global ∈ R^D`

#### 融合方式（残差式，稳定）

`z = LN(z_xyz + W1 z_feat + W2 z_global)`
得到每个点云的最终 embedding `z`。

---

## 5. Token 化与 `[RULE]` 显式规则变量（关键部分）

### 5.1 Token 序列（长度 10）

* 9 个位置 token（3×3）
* 1 个 `[RULE]` token（可学习向量 `w_rule`）

#### 位置编码

使用**可学习 row/col embedding**：

* `E_row[r]`，`E_col[c]`（r,c ∈ {0,1,2}）

#### token 构造

* 观测格子：`t_{r,c} = z_{r,c} + E_row[r] + E_col[c]`

* 缺失格子：`t_{r,c} = m_k + E_row[r] + E_col[c]`

  * 建议用 **3 个不同的 mask token** `m_0,m_1,m_2`（对应缺失的三个位置），让网络区分“哪一个缺失”

* 规则 token：`t_rule = w_rule`

最终序列：`[t_rule, t_0,0, t_0,1, ..., t_2,2]`

---

## 6. RuleFormer：推断规则向量 r，并预测目标格表示

### 6.1 结构

一个 Transformer Encoder（建议起步配置）：

* 层数 `L_rule = 6`
* hidden dim `D`（与 encoder 输出一致）
* heads：`8`（D=512）或 `12`（D=768）
* FFN ratio：4
* dropout：0.1（可调）

输入：10 tokens
输出：10 tokens `u_rule, u_{r,c}`

### 6.2 显式规则向量（规则瓶颈）

从 `[RULE]` 输出 token 得到规则向量：

* `r = LN(Wr * u_rule) ∈ R^D`

> 这一步就是“让模型知道规则”的关键：你把规则变成了一个显式向量，且它必须用于后续预测，否则损失会惩罚。

### 6.3 基于规则预测 target（必须显式依赖 r）

取 target 位置 token：`u_tgt = u_{2,2}`（target_position 以 meta 为准）

使用 AdaLN / FiLM 调制（强烈推荐）：

* `γ = MLPγ(r)`, `β = MLPβ(r)`（输出 R^D）
* `u_tgt_mod = LN(u_tgt) ⊙ (1+γ) + β`
* `z_hat_tgt = MLP_pred(u_tgt_mod) ∈ R^D`

你也可以同时预测所有缺失位置（不仅 target），但起步先只预测 target 即可。

---

## 7. VerifyFormer：候选验证 + 全局自洽 PE 打分（冲高分用）

> 仅用 `||ReLU(z_cand)-ReLU(z_hat_tgt)||` 也能做，但要“尽量高”，建议加 VerifyFormer，让错误候选会破坏整局自洽，从而更容易被排除。

### 7.1 对每个候选 j 构造“插入后矩阵 token”

* 把 target 位置 token 替换为候选 embedding：
  `t^j_{2,2} = z_cand[j] + E_row[2] + E_col[2]`
* 其它 token 同原始（观测格子用其 z，缺失用 mask）
* 规则 token初始化有两种策略：

  * **策略A（推荐）**：用 `u_rule` 作为初始 rule token（携带已推断规则）
  * 策略B：仍用 `w_rule`（更独立但可能更难）

### 7.2 VerifyFormer 结构

小 Transformer Encoder：

* 层数 `L_ver = 2~3`
* hidden `D`
* heads 与 RuleFormer 类似或更小

输出：`v^j_rule, v^j_{r,c}`

### 7.3 误差项（PE）定义与候选能量 E[j]

我们定义三种误差（可加权）：

1. **目标匹配误差（必须）**

* `e_tgt[j] = ReLU(z_cand[j]) - ReLU(z_hat_tgt)`
* `E_tgt[j] = ||e_tgt[j]||_2`

2. **观测自洽误差（推荐，提升上限）**
   对每个观测格子位置 `(r,c) ∈ Ω_obs`：

* 预测其 embedding：`z_tilde^j_{r,c} = MLP_rec(v^j_{r,c})`
* 误差：`||ReLU(z_{r,c}) - ReLU(z_tilde^j_{r,c})||_2`
* 总和：`E_obs[j] = Σ_{obs} ...`

3. **规则一致性误差（可选）**

* 从 VerifyFormer 的 rule token 得到 `r^j = LN(Wr * v^j_rule)`
* `E_rule[j] = ||r^j - r||_2`

总能量：

* `E[j] = α E_tgt[j] + β E_obs[j] + γ E_rule[j]`
  得分：
* `score[j] = -E[j]`（或再喂一个小 MLP 输出 score）

推荐起步权重：

* `α=1.0, β=0.5, γ=0.2`

---

## 8. 训练目标（Loss）与训练策略（两阶段微调预训练 encoder）

### 8.1 主损失：4 选 1 交叉熵

* `L_ce = CE(scores, gt_index)`

### 8.2 PE margin 辅助（强烈建议）

让正确候选能量更小、错误候选至少大于阈值 τ：

* `E_pos = E[gt]`
* `E_neg = {E[j], j≠gt}`
  [
  L_{margin} = E_{pos}^2 + \sum_{neg} \max(0,\tau - E_{neg})^2
  ]
  总损失：
* `L = L_ce + λ L_margin`（λ=0.1 起步，τ=0.7 起步，按能量尺度调）

### 8.3 （可选但很香）规则监督：让 r 可解释、可分

你的 meta 里有 `rule_template / matrix_relation / rule_id` 等字段时，可加辅助分类头：

* `p = softmax(W_cls r)`
* `L_rule = CE(p, y_rule)`
  总损失：
* `L_total = L_ce + λ L_margin + μ L_rule`（μ=0.1~0.3）

这通常能显著提升泛化和收敛速度（模型更“知道自己在学规则”）。

### 8.4 两阶段训练（强烈推荐）

**阶段A：冻结预训练点云主干**
训练：

* 特性分支、全局分支
* RuleFormer、VerifyFormer
* pred/rec head、score head、(可选) rule cls head

优点：稳定、不易把预训练搞崩。

**阶段B：参数高效微调（PEFT/LoRA/解冻后几层）**
只微调预训练 encoder 的一小部分：

* 解冻最后 2~4 个 block，或加 LoRA/adapter/prompt（更省显存、更不易过拟合）
  学习率建议：
* 头部：`1e-4 ~ 3e-4`
* 预训练主干（PEFT/LoRA）：`1e-5 ~ 3e-5`

---

## 9. 训练资源与性能建议（你这题的真实开销点）

### 9.1 为什么它比普通分类更吃资源

每个样本要编码：

* 6 个观测格子 + 4 个候选 = **10 个点云**
  点云 encoder 才是大头；RuleFormer/VerifyFormer token 只有 10 个，开销很小。

### 9.2 推荐配置（经验上可行的起步）

* GPU 8GB：`N0=1024`，batch 8~16（混合精度）
* GPU 12~16GB：`N0=1024/2048`，batch 16~32
* 强烈建议：**AMP(fp16/bf16)** + **梯度累积** + **预处理二进制缓存**

### 9.3 性能优化（很关键）

* **缓存点云 encoder 输出**（尤其阶段A冻结主干时）：
  把每个 `.ply` 的 `z_xyz` 预计算并保存，训练时直接读 embedding，速度会飞起。
* dataloader 多进程、pin_memory
* 统一采样点数 N0

---

## 10. 评估与分析（如何确认模型真的在学规则）

### 10.1 必做指标

* 总准确率（Top-1）
* 按 `rule_template` 分组准确率
* 按 `point_cloud_row_perturbation.selected_type` 分组准确率（density/jitter/quantize/outlier）
* 按 `distractor_types` 分组准确率（看是否被某类干扰项骗）

### 10.2 诊断模型是否“乱做”

* 看 `r` 的可分性：对规则标签做线性 probe（如果你用了 L_rule 更明显）
* 看 PE 分布：正确候选的 `E_pos` 是否显著低于错误候选
* 看 “插入错误候选后 E_obs 是否明显上升”：这能证明 VerifyFormer 在用“全局自洽”筛错

---

## 11. 工程模块划分（建议代码结构）

1. `datasets/pcrar_dataset.py`

   * 读取 meta.json
   * 组装 9 格点云路径 + 4 候选路径
   * 读取/缓存二进制点云
   * 输出：`{xyz_local, xyz_global, feat, global_stats, masks, candidates, gt}`

2. `models/encoder/`

   * `pretrained_encoder.py`（Point-MAE / Point-BERT wrapper）
   * `feat_encoder.py`（PointNet 轻量分支）
   * `global_mlp.py`（统计特征分支）
   * `fusion.py`

3. `models/reasoner/`

   * `ruleformer.py`
   * `verifyformer.py`
   * `heads.py`（pred/rec/score/rule_cls）

4. `train.py / eval.py`

   * 两阶段训练流程
   * AMP、梯度累积、日志（wandb/tensorboard）

---


